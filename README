# Como iniciar el proyecto

1. Instalar las dependencias "npm i", usar una version de node 20 o superior
2. Iniciar tu docker en tu computador, correr el comando `docker-compose up -d" para inicar el contenedor
3. npm run start para iniciar la aplicacion y comenzar con el scrapeo

